# v8 — 主題偵測升級 + 產業鏈地圖（文章 3 變成可連載研究）

## 目標
把文章 3 做成可連載的「主題研究」，並讓主題選擇更穩定：
- 主題偵測：從規則法升級到 embedding clustering（或混合）
- 產業鏈地圖：用 deterministic 方式生成一張「產業鏈圖」（SVG/PNG）
- 文章 3 固定包含：產業鏈地圖 + 代表股表格 + 展望情境

---

## 範圍（Scope）
### 會做
- Theme detection v2：
  - 以新聞 headline + 摘要（可含 transcript signals）做 embedding
  - 聚類後取最大 cluster 作為今日主題
  - 並輸出「為什麼是這個主題」的可解釋訊號（cluster size、涵蓋 tickers）
- Industry chain map v1（deterministic）
  - LLM 先產出結構化 JSON（nodes/edges/labels）
  - 再用程式渲染成 Mermaid/Graphviz/SVG → PNG
  - 圖中不得出現不確定數字（以關係/角色為主）
- 文章 3 更新：固定插入產業鏈地圖圖片 + 表格

---

## 交付物（Deliverables）
- `features/theme_clustering.py`
- `llm/extract_theme_graph_json.py`
- `render/theme_graph_renderer.py`
- `publish/image_upload.py`（若尚未穩定，這版要補強）
- `templates/article3.md` 更新

---

## 驗收標準（Definition of Done）
- [ ] 主題偵測每天都能輸出一個主題（若資料不足則 fallback 到規則法）
- [ ] 文章 3 每天都有一張產業鏈地圖（或 fallback：至少有表格）
- [ ] 圖像生成 deterministic（可重現）

---

## 下一版（v9）會加什麼
- QA Gate 完整化 + 自動測試（把關後再發佈）

